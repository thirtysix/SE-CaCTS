# tykky conda-container spec for the SE-CaCTS pilot on Roihu (built on the LOGIN node).
# deepTools brings multiBigwigSummary; the rest run 05_normalize_pca.py.
channels: [conda-forge, bioconda]
dependencies:
  - python=3.12
  - deeptools
  - numpy
  - pandas
  - scipy
  - matplotlib
  - bedtools
